# Flask Configuration
SECRET_KEY=your-secret-key-here-change-in-production
FLASK_ENV=production
PORT=5000

# Optional: Redis (if using Redis instead of in-memory cache)
# REDIS_URL=redis://localhost:6379/0